RewriteEngine On

# Serve static files that are in here
RewriteCond "static/%{REQUEST_URI}" -f
RewriteRule "^(.+)" "static/$1" [END]

# Send everything else to index.php
RewriteRule ^ index.php [QSA,END]
