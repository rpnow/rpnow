autocert
create RP: title entry

Update guides to download .tar.gz, extract, and run install.sh
    export RPINSTALL=$(mktemp -d) && tar -zxvf <(wget -qO- https://github.com/rpnow/rpnow/releases/download/VERSION_NUM_HERE/rpnow-linux.tar.gz) -C $RPINSTALL && $RPINSTALL/install.sh && rm $RPINSTALL
Update guide to recommend Google's standard network tier over premium

Verify that JSON imports don't time out, even on huge RP's

---

Netflix-style user picker login, with (+ new user)
Create new user

Create New-RP Dialog (title, banner)
Creating an RP adds the RP to your account
"Invite people?" banner at top of RP
Invite option in the side drawer
Invite users to RP dialog (adds to their account)
403 if you weren't invited yet

Remove concept of rp slugs

Pretty homepage
Remove <a href>, switch to <router-link>
Remove RP shortcuts
Remake create RP area

Enable/disable create&import: Admin API
Enable/disable create&import: Admin CLI
Enable/disable user registration: Admin API
Enable/disable user registration: Admin CLI
Disable user account: Admin API
Disable user account: Admin CLI
Change user password: Admin API
Change user password: Admin CLI
Kick user from RP: Admin API (should kill WS connections)
Kick user from RP: Admin CLI

Verify user can create
Verify user can import
Verify user can register

Import should give better feedback (ie upload progress, then how many msgs are processed)

Error message: Import rp start
Error message: Import rp progress

extract some css into a public .css file
expose rp.css & favicons

---

Move away from XID, which is vulnerable to Y2038 problem

Error message: New rp
Error message: 404
Error message: 403
Error message: 410 gone
Error message: Update fetch 404
Error message: Update fetch 403
Error message: Update fetch 410 gone
Error message: Chara dialog
Error message: Image dialog
Error message: Message box

Cache invalidation
Dynamic archives
Drawer animation
Options: note noise (+persistence)
Put the 'desc' on the page somewhere?
API unit tests
Chara drawer organization
Recent charas (+persistence)
Logging
